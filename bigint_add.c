#include "bigint.h"


/**
 * @brief adds two big interger numbers as a string
 *
 * @param[char *] num1 the first number as a string
 * @param[char *] num2 the second number as a string
 * @return the reuslt as a string of the adding the two numbers
 */
BigInt bigint_add(BigInt num1, BigInt num2)
{
    if (!num1 || !num2)
        return (NULL);
    return ("TODO");
}
